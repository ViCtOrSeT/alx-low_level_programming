#include "holberton.h"

/**
 * add - adds two intergers and returns the result
 * @v: int to be added to k
 * @k: int to be added to v
 * Return: sum of v and k
 */
int add(int v, int k)
{
int o;
o = v + k;

return (o);
}
