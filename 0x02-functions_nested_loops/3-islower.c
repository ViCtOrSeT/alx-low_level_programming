#include "holberton.h"

/**
 * _islower - checks for lower case character
 * @c: variable to determine if input is lower
 * main - check for lowercase character
 * Return: 1 if c is lowercase, 0 if otherwise
 */
int _islower(int c)
{
return (c >= 'a' && c <= 'z');
}
